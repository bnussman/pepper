import { Text, NativeBaseProvider } from 'native-base';
import React from 'react';

export default function App() {
    return (
        <NativeBaseProvider>
            <Text bold color="green">testing dggewqjgjdk</Text>
        </NativeBaseProvider>
    );
}
